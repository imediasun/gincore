DirectoryIndex index.php
 
<IfModule mod_rewrite.c>
    RewriteEngine On
#    RewriteBase /sites/restore/manage/
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule (.*) index.php [L]
</IfModule>
